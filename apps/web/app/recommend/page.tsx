import { RouteHero } from "@/components/route-hero";

export default function RecommendPage() {
  return (
    <RouteHero
      title="Recommendations"
      description="Generate recommendations grounded in your personal context graph and inspect the receipts behind every result."
    />
  );
}
