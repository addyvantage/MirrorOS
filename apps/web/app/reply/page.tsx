import { RouteHero } from "@/components/route-hero";

export default function ReplyPage() {
  return (
    <RouteHero
      title="Reply Composer"
      description="Draft context-aware responses in your voice while preserving references to the source traces MirrorOS used."
    />
  );
}
